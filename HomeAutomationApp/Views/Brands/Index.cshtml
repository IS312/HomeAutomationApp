@model IEnumerable<HomeAutomationApp.Models.Brand>

@{
    ViewData["Title"] = "Index";
}

<h1>View All Brand</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Add New Brand</a>
</p>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BrandName)
            </th>
            <th> Operations </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BrandName)
            </td>
            <td>
                <a asp-action="Edit" class="btn" asp-route-id="@item.BrandID">Edit</a> |
                <a asp-action="Details" class="btn" asp-route-id="@item.BrandID">Details</a> |
                <a asp-action="Delete" class="btn" asp-route-id="@item.BrandID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
